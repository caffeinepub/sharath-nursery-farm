import { Button } from '@/components/ui/button';

export function WorkerForm() {
  return (
    <div className="space-y-4">
      <Button className="bg-nursery-secondary">Add Worker</Button>
    </div>
  );
}
