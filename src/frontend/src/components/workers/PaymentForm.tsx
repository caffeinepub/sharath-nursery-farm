import { Button } from '@/components/ui/button';

export function PaymentForm({ workers }: any) {
  return (
    <div className="space-y-4">
      <Button className="bg-nursery-secondary">Record Payment</Button>
    </div>
  );
}
